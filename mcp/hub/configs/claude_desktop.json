{
  "_instructions": [
    "1. Open: %APPDATA%\\Claude\\claude_desktop_config.json  (Windows)",
    "          ~/Library/Application Support/Claude/claude_desktop_config.json  (macOS)",
    "2. Merge the mcpServers block below into the existing JSON.",
    "3. Replace the path in 'args' with the ABSOLUTE path to hub/main.py on your machine.",
    "4. Restart Claude Desktop."
  ],
  "mcpServers": {
    "local-mcp-hub": {
      "command": "python",
      "args": ["C:/Users/YourName/Desktop/maimoon/mcp/hub/main.py"],
      "env": {}
    }
  }
}
